import FlyShooterCanvas from "@/components/game-wrapper/FlyShooterCanvas";

export default function FlyShooterPage() {
  return (
    <main style={{ width: '100%', height: '100vh', overflow: 'hidden' }}>
      <FlyShooterCanvas />
    </main>
  );
}
