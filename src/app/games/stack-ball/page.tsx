import StackBallCanvas from "@/components/game-wrapper/StackBallCanvas";

export default function StackBallPage() {
  return (
    <main style={{ width: '100vw', height: '100vh', overflow: 'hidden' }}>
      <StackBallCanvas />
    </main>
  );
}
